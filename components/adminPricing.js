import styled from "styled-components";

const AdminPricing = (props) => {
  return <Container></Container>;
};

export default AdminPricing;

const Container = styled.div`
  width: calc(100% - 280px);
  margin-left: auto;
  @media (max-width: 1024px) {
    width: 100%;
  }
`;
