import styled from "styled-components";

const Clients = (props) => {
  return <Container>Client Logos</Container>;
};

export default Clients;

const Container = styled.div`
  width: 100%;
  height: 500px;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: row;
`;
